package edu.cornell.library.integration.utilities;

public class BoxScratch {
	/*
	BoxFolder rootFolder = BoxFolder.getRootFolder(api);
	for (BoxItem.Info itemInfo : rootFolder) {
		System.out.format("[%s] %s\n", itemInfo.getID(), itemInfo.getName());
	}

	//[229388968760] test json
	//[229196671078] test triggers
	BoxFolder triggerFolder = new BoxFolder(api, "229196671078");

	for (BoxItem.Info itemInfo : triggerFolder) {
		System.out.format("[%s] %s\n", itemInfo.getID(), itemInfo.getName());
		if ( itemInfo.getID().equals(file_id) ) {
			System.out.println("this is the file");
		}
	}
*/

}
